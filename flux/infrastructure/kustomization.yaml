## The manifest with all infrastructure-level applications.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - aws-load-balancer-controller
  - aws-vpc-cni
  - cilium
  - metrics-server
  - kube-proxy
