apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-demo
  namespace: efs-demo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi
