apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-demo
  namespace: efs-demo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-static
  resources:
    requests:
      storage: 5Gi
